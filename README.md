# MEAN Skeleton

## Build & development

- Install dependencies for both client side and server side
```
npm install
cd server
npm install
cd ..
```
- Start server
```
grunt serve
```
- Navigate to http://localhost:3000

